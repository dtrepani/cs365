
//This is registering a future event that will occur
//The first arg is a function to execute
//The second arg is how long from now (in milliseconds) this function should be executed.
var timerObject = setTimeout(function() {
	console.log("This happened a few seconds later....");
	clearTimeout(timerObject);
}, 1500);


//setInterval() does the same thing, only *every* that amount of time until you call:

clearTimeout(timerObject);


